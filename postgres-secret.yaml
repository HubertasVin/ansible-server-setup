apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: detourpilot
  labels:
    app.kubernetes.io/part-of: detourpilot
    app.kubernetes.io/component: postgres
type: Opaque
stringData:
  # Override in production — see deploy notes in README.
  POSTGRES_USER: detourpilot
  POSTGRES_PASSWORD: detourpilot
  POSTGRES_DB: detourpilot
