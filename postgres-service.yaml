apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: detourpilot
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: detourpilot
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: detourpilot
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
